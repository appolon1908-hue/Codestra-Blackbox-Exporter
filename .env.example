# Runtime image identity is fixed by codestra/release/runtime-image.lock.json
# and each reviewed Compose source. It is not caller-overridable.
CODESTRA_PROMETHEUS_BLACKBOX_NETWORK=codestra-prometheus-blackbox
