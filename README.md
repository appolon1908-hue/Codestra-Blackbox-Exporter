# Codestra Blackbox Exporter

Authoritative repository for Codestra HTTP, HTTPS, TCP, DNS, ICMP, and gRPC probe execution through Blackbox Exporter.

This bootstrap commit creates repository history only. Runtime configuration must be introduced through `feature/* -> development` pull requests and promoted through `test`, `staging`, `production`, and `main`. No deployment is authorized by this file.
